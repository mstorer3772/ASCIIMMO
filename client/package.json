{
  "name": "asciimmo-client",
  "version": "0.1.0",
  "description": "ASCIIMMO browser client",
  "private": true,
  "scripts": {
    "build": "browserify app.js -o bundle.js",
    "watch": "watchify app.js -o bundle.js -v",
    "serve": "node server.js"
  },
  "dependencies": {
    "google-protobuf": "^3.21.2",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "browserify": "^17.0.0",
    "watchify": "^4.0.0"
  }
}